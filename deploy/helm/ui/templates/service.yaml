apiVersion: v1
kind: Service
metadata:
  name: trustsystem-ui
spec:
  selector:
    app: trustsystem-ui
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http


